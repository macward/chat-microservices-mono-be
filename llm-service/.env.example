# Configuración del Servicio LLM
# Copia este archivo a .env y ajusta los valores según tu entorno

# ===========================================
# CONFIGURACIÓN DE LA APLICACIÓN
# ===========================================

# Información básica de la aplicación
LLM_SERVICE_APP_NAME=LLM Service
LLM_SERVICE_APP_VERSION=1.0.0
LLM_SERVICE_DEBUG=false

# Configuración del servidor
LLM_SERVICE_HOST=0.0.0.0
LLM_SERVICE_PORT=8000

# ===========================================
# CONFIGURACIÓN DE LM STUDIO
# ===========================================

# Conexión a LM Studio
LLM_SERVICE_LM_STUDIO_HOST=localhost
LLM_SERVICE_LM_STUDIO_PORT=1234
LLM_SERVICE_LM_STUDIO_TIMEOUT=30
LLM_SERVICE_LM_STUDIO_MAX_RETRIES=3

# Modelo por defecto
LLM_SERVICE_DEFAULT_MODEL=Dirty-Muse-Writer-v01-Uncensored-Erotica-NSFW-Q6_K-GGUF/dirty-muse-writer-v01-uncensored-erotica-nsfw-q6_k.gguf

# ===========================================
# CONFIGURACIÓN DE LA API
# ===========================================

# Límites de la API
LLM_SERVICE_MAX_REQUEST_SIZE=10000
LLM_SERVICE_RATE_LIMIT_REQUESTS=100

# ===========================================
# CONFIGURACIÓN DE LOGGING
# ===========================================

# Nivel y formato de logs
LLM_SERVICE_LOG_LEVEL=INFO
LLM_SERVICE_LOG_FORMAT=json

# ===========================================
# CONFIGURACIÓN DE SEGURIDAD
# ===========================================

# CORS - Orígenes permitidos (separados por comas para múltiples)
# Para desarrollo: ["*"]
# Para producción: ["https://mi-dominio.com", "https://mi-app.com"]
LLM_SERVICE_ALLOWED_ORIGINS=["*"]

# Hosts de confianza (separados por comas para múltiples)
# Para desarrollo: ["*"]
# Para producción: ["mi-dominio.com", "mi-app.com"]
LLM_SERVICE_TRUSTED_HOSTS=["*"]

# ===========================================
# NOTAS IMPORTANTES
# ===========================================

# 1. Para producción, cambia DEBUG a false
# 2. Configura ALLOWED_ORIGINS y TRUSTED_HOSTS específicamente para tu dominio
# 3. Ajusta LM_STUDIO_HOST y LM_STUDIO_PORT según tu configuración de LM Studio
# 4. El LOG_FORMAT puede ser "json" o "text"
# 5. Ajusta RATE_LIMIT_REQUESTS según tus necesidades de tráfico
# 6. Para múltiples valores en listas, usa formato JSON: ["valor1", "valor2"]